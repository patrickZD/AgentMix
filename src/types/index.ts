// Barrel for the app's types. Components import these via '@/types'. The
// canonical domain types (Skill, SourceProject, ...) are generated in
// ./generated and re-exported through ./app; the UI-only composites live in
// ./app alongside them.
export * from './app';
