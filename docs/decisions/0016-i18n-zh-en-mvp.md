# i18n 在 MVP 即支持中文 + 英文，作为架构红线

> 原决策 16（DESIGN.md v0.6 设计决策附录）

i18n 是典型的"早不立，后期改造极贵"的架构层——硬编码字符串散落在每个 React 组件中，事后改造要逐个修改全部 UI 文件。MVP 即引入 `react-i18next`，所有用户可见字符串通过 `t()` lookup，CI 校验中英资源文件 key 集合一致。MVP 范围限定中英两种，覆盖核心用户群体；其他语种（日/韩等）视 v1.0+ 社区需求添加，i18n 架构本身无需改动。
