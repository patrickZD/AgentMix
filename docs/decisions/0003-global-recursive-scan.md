# 扫描策略改为全局递归而非固定路径

> 原决策 3（DESIGN.md v0.6 §13 附录）

原设计依赖固定路径前缀（`.claude/skills/`、`.cursor/skills/` 等），这要求用户在导入前将项目文件夹放置到特定目录，与实际使用场景（直接从 GitHub 下载后导入）不符。改为全局递归扫描后，工具能识别任意目录结构中的 SKILL.md，代价是需要更严格的合法性校验来避免误识别。
